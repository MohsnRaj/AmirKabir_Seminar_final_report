% !TeX root = ../../../main.tex
% !TeX program = xelatex

\documentclass[../../main.tex]{subfiles}
\begin{document}

\subsubsection{هماهنگ‌سازی ضمنی کانال با تکیه بر \lr{beamforming}: نالی‌فیکیشنِ تداخل \lr{gNB} روی \lr{AP} \lr{Wi-Fi}}
\label{sssec:icc_beamforming_phy}
متن پیش فرض


\end{document}
